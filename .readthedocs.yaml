# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# Use the same Python packages you have in your Dockerfile
python:
  install:
  - requirements: requirements.txt

mkdocs:
  configuration: mkdocs.yml